# For pg_regress --temp-instance (default) mode.
# overlay_branch hooks are installed in _PG_init(), so the library MUST be
# loaded at postmaster-start time — otherwise Step 4 D/U/D redirect silently
# writes through to Main and our Main-zero-pollution assertions fail.
shared_preload_libraries = 'overlay_branch'
